<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class=" p-6">
  <header class="fixed top-0 left-0 w-full bg-white shadow-md z-10 h-20">
    <div class="container h-full min-w-full flex justify-between items-center p-4">
      <!-- Logo Wrapper -->
      <div class="h-full w-44  ">
        <img 
          class="h-full w-full object-cover" 
          src="/images/knoqlogicologico.svg" 
          alt="Company Logo" 
        />
      </div>
          <!-- Desktop Navigation -->
          <nav class="hidden md:flex space-x-6">
            <a href="/user/<%= currentLoginUser %>/dashboard" class="text-gray-800 hover:text-blue-500">Dashboard</a>
            <a href="/logout" class="text-gray-800 hover:text-blue-500">Logout</a>
          </nav>
          
          <!-- Mobile Menu Button -->
          <button id="menu-toggle" class="md:hidden text-gray-800 focus:outline-none">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" class="w-6 h-6">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
        
        <!-- Mobile Menu -->
        <div id="mobile-menu" class="hidden bg-gray-100 md:hidden">
          <a href="/user/<%= currentLoginUser%>/dashboard"  class="block px-5 py-2 text-gray-800 hover:bg-blue-500 hover:text-white">Dashboard</a>
          <a href="/logout" class="block px-5 py-2 text-gray-800 hover:bg-blue-500 hover:text-white">Logout</a>
        </div>
      </header>
   <div class="overflow-x-auto bg-white shadow-md rounded-lg  mt-20">
    <table class="w-full border-collapse border border-gray-200">
        <thead class="bg-blue-500 text-white">
          <tr>
            <th class="py-1 px-4 text-center">ID</th>
            <th class="py-1 px-4 text-center">Name</th>
            <th class="py-1 px-4 text-center">Email</th>
            <th class="py-1 px-4 text-center">Role</th>
            <th class="py-1 px-4 text-center">Current Address</th>
            <th class="py-1 px-4 text-center">Permanent Address</th>
            <th class="py-1 px-4 text-center">Phone Number</th>
            <th class="py-1 px-4 text-center">Emergency Contact</th>
            <th class="py-1 px-4 text-center">Actions</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(user => { %>
            <tr>
              <td class="py-3 px-6 text-center"><%= user.user_id %></td>
              <td class="py-3 px-6 text-center"><%= user.first_name %> <%= user.last_name %></td>
              <td class="py-3 px-6 text-center"><%= user.email %></td>
              <td class="py-3 px-6 text-center"><%= user.roleName || 'N/A' %></td>
              <td class="py-3 px-6 text-center"><%= user.currentAddress || 'N/A' %></td>
              <td class="py-3 px-6 text-center"><%= user.permanentAddress || 'N/A' %></td>
              <td class="py-3 px-6 text-center"><%= user.phoneNumber || 'N/A' %></td>
              <td class="py-3 px-6 text-center"><%= user.emergencyContact || 'N/A' %></td>
              <td class="py-3 px-6 text-center">
                <a href="/user/admin/users/<%= user.user_id %>/edit" class="text-blue-500 hover:underline">Update</a>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>
   </div>
    
      
      
</body>
</html>