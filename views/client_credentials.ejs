<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Client Credentials</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br bg-gray-100 min-h-screen flex items-center justify-center">
    
  <header class="fixed top-0 left-0 w-full bg-white shadow-md z-10 h-20">
    <div class="container h-full min-w-full flex justify-between items-center p-4">
      <!-- Logo Wrapper -->
      <div class="h-full w-44  ">
        <img 
          class="h-full w-full object-cover" 
          src="/images/knoqlogicologico.svg" 
          alt="Company Logo" 
        />
      </div>
          <nav class="hidden md:flex space-x-6">
            <a href="/user/<%= isCurrentUser %>/dashboard"
              class="text-gray-800 hover:text-blue-500">Dashboard</a>
            <a href="/users/login"
              class="text-gray-800 hover:text-blue-500">Logout</a>
          </nav>
          <button id="menu-toggle"
            class="md:hidden text-gray-800 focus:outline-none">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none"
              viewBox="0 0 24 24" stroke="currentColor" class="w-6 h-6">
              <path stroke-linecap="round" stroke-linejoin="round"
                stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
        <div id="mobile-menu" class="hidden bg-gray-100 md:hidden">
          <a href="/user/<%= isCurrentUser %>/dashboard"
            class="block px-5 py-2 text-gray-800 hover:bg-blue-500 hover:text-white">Dashboard</a>
          <a href="/users/login"
            class="block px-5 py-2 text-gray-800 hover:bg-blue-500 hover:text-white">Logout</a>
        </div>
      </header>


    <div class="bg-white shadow-lg rounded-xl p-8 max-w-md w-full">
        <h2 class="text-3xl font-bold text-blue-600 mb-6 text-center">Client Credentials</h2>

        <div class="space-y-6">
            <!-- Client Credential Card -->
            <div class="bg-blue-50 border-l-4 border-blue-500 shadow-sm rounded-lg p-6">
                <p class="text-lg text-gray-700 mb-2">
                    <span class="font-semibold text-blue-600">Email:</span> <%= client_credential_email %>
                </p>
                <p class="text-lg text-gray-700 mb-2">
                    <span class="font-semibold text-blue-600">Password:</span> <%= client_credential_password %>
                </p>
                <p class="text-lg text-gray-700">
                    <span class="font-semibold text-blue-600">Type:</span> <%= client_credential_type %>
                </p>
            </div>
        </div>

        <div class="mt-8 text-center">
            <a href="/request/request-credential" 
               class="inline-block bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-6 rounded-lg shadow-md transition-transform transform hover:scale-105">
                Back to Request Credential
            </a>
        </div>
    </div>
  
</body>
</html>
