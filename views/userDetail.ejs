<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EDIT USER</title>
    <script src="https://cdn.tailwindcss.com"></script>
    
</head>

<body class="bg-gray-100">
    
    <!-- Main Wrapper -->
    <div class="relative flex min-h-screen bg-gray-100">
        <!-- Menu Button (Visible only on smaller screens) -->
        <button id="toggleNav"
            class="lg:hidden fixed top-4 left-4 z-20 bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 transition">
            ☰ 
        </button>

        <!-- Navigation Menu -->
        <nav id="navMenu"
            class="w-64 bg-white p-6 space-y-4 shadow-md fixed top-0 left-0 h-full transform -translate-x-full lg:translate-x-0 lg:static transition-transform duration-300 z-50">
            <!-- Close Button (Visible only on smaller screens) -->
            <button id="closeNav" class="lg:hidden absolute top-4 right-4 text-gray-600 hover:text-gray-800">
                ✕ Close
            </button>

            <div class="text-center">
                <!-- Profile Image -->
                <img src="https://via.placeholder.com/100" alt="Profile Picture"
                    class="w-16 sm:w-24 h-16 sm:h-24 mx-auto rounded-full mb-4 shadow-md">
                <p class="text-md sm:text-lg font-bold text-gray-800 capitalize"><%= user.first_name%> <%= user.last_name%></p>
            </div>

            <!-- Navigation Links -->
            <ul class="space-y-2">
                <li>
                    <a href="/user/<%= user.user_id %>/dashboard"
                        class="flex items-center px-2 sm:px-4 py-2 text-gray-600 hover:bg-gray-200 rounded-md transition">
                        <span>🏠</span>
                        <span class="ml-2 sm:ml-3">Dashboard</span>
                    </a>
                </li>
                <li>
                    <a href="/user/<%= user.user_id %>"
                        class="flex items-center px-2 sm:px-4 py-2 bg-blue-100 text-blue-600 font-medium rounded-md transition">
                        <span>📋</span>
                        <span class="ml-2 sm:ml-3">User Details</span>
                    </a>
                </li>
                <li>
                    <a href="/user/<%= user.user_id %>/edit"
                        class="flex items-center px-2 sm:px-4 py-2 text-gray-600 hover:bg-gray-200 rounded-md transition">
                        <span>📋</span>
                        <span class="ml-2 sm:ml-3">Update Profile</span>
                    </a>
                </li>
                <% if (user.user_role_id === 2 || user.is_admin===1 ) { %>
                <li>
                    <a href="/leads/create"
                        class="flex items-center px-2 sm:px-4 py-2 text-gray-600 hover:bg-gray-200 rounded-md transition">
                        <span>📋</span>
                        <span class="ml-2 sm:ml-3">Add leads</span>
                    </a>
                </li>
                <%} %>
                <% if (user.user_role_id === 2 || user.is_admin===1 ) { %>
                <li>
                    <a href="/leads"
                        class="flex items-center px-2 sm:px-4 py-2 text-gray-600 hover:bg-gray-200 rounded-md transition">
                        <span>📋</span>
                        <span class="ml-2 sm:ml-3">View Leads</span>
                    </a>
                </li>
                <%} %>
                <li>
                    <a href="/logout"
                        class="flex items-center px-2 sm:px-4 py-2 text-gray-600 hover:bg-gray-200 rounded-md transition">
                        <span>🚪</span>
                        <span class="ml-2 sm:ml-3">Logout</span>
                    </a>
                </li>
            </ul>
        </nav>

        <!-- Main Content -->
        <div class="w-full sm:w-3/5 md:w-full lg:w-full p-6 bg-gray-50 rounded-lg">
            <!-- Header -->
            <h1 class="text-3xl sm:text-4xl font-bold text-gray-800 mb-6 text-center tracking-wider">User Details</h1>
        
            <!-- Personal Information -->
            <section class="mb-6">
                <h2 class="text-2xl sm:text-3xl font-semibold mb-2">Personal Information</h2>
                <ul class="list-disc pl-6 space-y-2">
                    <li><strong>First Name:</strong> <%= user.first_name %></li>
                    <li><strong>Last Name:</strong> <%= user.last_name %></li>
                    <li><strong>User ID:</strong> <%= user.user_id %></li>
                </ul>
            </section>
        
            <!-- Contact Information -->
            <section class="mb-6">
                <h2 class="text-2xl sm:text-3xl font-semibold mb-2">Contact Information</h2>
                <ul class="list-disc pl-6 space-y-2 text-wrap">
                    <li><strong>Email:</strong> <%= user.email %></li>
                </ul>
            </section>
        
            <!-- Other Information -->
            <section class="mb-6">
                <h2 class="text-2xl sm:text-3xl font-semibold mb-2">Other Information</h2>
                <ul class="list-disc pl-6 space-y-2">
                    <li><strong>Role:</strong> <%= user.roleName %></li>
                    <li><strong>Joined On:</strong> <%= new Date(user.created_at).toLocaleString() %></li>
                </ul>
            </section>
        
            <!-- Conditional Rendering for Current User -->
            <div class="mt-6 text-center">
                <% if (isCurrentUser) { %>
                    <a href="/users/<%= user.user_id %>/edit"
                        class="inline-block bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition">
                        Edit Profile
                    </a>
                <% } %>
            </div>
        </div>
        
    </div>

    <!-- JavaScript for Menu Toggle -->
    <script src="/javascripts/dashboard.js"></script>
    <script src="/javascripts/userDetails.js"></script>
</body>




</html>